<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/tagsItemWidth"
    android:layout_height="@dimen/tagsItemHeight">

    <ImageView
        android:id="@+id/tags_item_ItemImage"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:scaleType="centerCrop" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/tagsItemTextVerticalMargin"
        android:layout_gravity="center"
        android:textSize="@dimen/tagsItemTextColor"
        android:padding="@dimen/tagsItemTextPadding"
        android:textColor="#ffffff"
        android:shadowColor="#000000"
        android:shadowRadius="20"
        android:id = "@+id/tags_item_ItemText" />

</FrameLayout>