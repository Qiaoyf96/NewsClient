<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_progressbar_loaded"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tv"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/searchVerticalMargin"
        android:layout_marginTop="@dimen/searchVerticalMargin"
        android:gravity="center_vertical"
        android:text="No more news"
        android:textAlignment="viewEnd"
        android:textAllCaps="false"
        android:textColor="#888888"
        android:textSize="@dimen/searchTextSize" />
</LinearLayout>